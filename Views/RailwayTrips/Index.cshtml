@model _Tripfinity.Views.PaginatedList<_Tripfinity.Models.Tables.RailwayTrip>
@{
    Layout = "_DashboardLayout";
    ViewData["Title"] = "Railway Trips";
}
@section Styles {
    <link rel="stylesheet" href="~/css/bus-trips.css" />
    
}

<section class="page-title-section">
    <div class="breadcrumbs">Passenger Dashboard > Book a Train</div>
    <h1>Available Railway Trips</h1>
    <p>Real-time inter-city rail schedules.</p>
</section>

<div id="trips-container">
    @if (!Model.Any())
    {
        <p>No trips available right now.</p>
    }
    else
    {
        <div class="trip-options">
            @foreach (var trip in Model)
            {
                var formattedDate = trip.DepartureTime.ToString("MM/dd/yyyy");
                var formattedTime = trip.DepartureTime.ToString("hh:mm tt");
                <div class="trip-card">
                    <div class="trip-card-icon">🚆</div>
                    <div class="route-display">
                        <h2>@trip.From</h2>
                        <span class="route-arrow">→</span>
                        <h2>@trip.Destination</h2>
                    </div>
                    <div class="meta-grid">
                        <div class="meta-item">
                            <span class="meta-label">Departure</span>
                            <span class="meta-value">@formattedDate<br><span style="color:var(--text-muted); font-weight:400;">@formattedTime</span></span>
                        </div>
                        <div class="meta-item">
                            <span class="meta-label">Price</span>
                            <span class="meta-value">₦@trip.Price.ToString("N0")</span>
                        </div>
                        <div class="meta-item">
                            <span class="meta-label">Seats</span>
                            <span class="meta-value">@trip.AvailableSeats/@trip.TotalSeats <span class="tag-full">Available</span></span>
                        </div>
                    </div>
                    <a href="/Trips/Railway/@trip.Id/Book" class="btn-book-action">Book Now ></a>
                </div>
            }
        </div>
    }
</div>

@if (Model.TotalPages > 1)
{
    <div class="pagination">
        @if (Model.HasPreviousPage)
        {
            <a href="/Trips/Railway?page=@(Model.PageIndex - 1)" class="page-btn">Previous</a>
        }
        @for (int i = 1; i <= Model.TotalPages; i++)
        {
            if (i == Model.PageIndex)
            {
                <span class="page-num active">@i</span>
            }
            else
            {
                <a href="/Trips/Railway?page=@i" class="page-num">@i</a>
            }
        }
        @if (Model.HasNextPage)
        {
            <a href="/Trips/Railway?page=@(Model.PageIndex + 1)" class="page-btn">Next ></a>
        }
    </div>
}