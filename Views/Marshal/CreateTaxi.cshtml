@model _Tripfinity.Models.Data.Requests.CreateTaxiTripRequest
@{
    Layout = "_MarshalLayout";
    ViewData["Title"] = "Create Taxi Trip";
}
@section Styles {
    <link rel="stylesheet" href="~/css/marshal-portal.css" />
    <link rel="stylesheet" href="~/css/marshal-create.css" />
    
}

<div class="content">
    <div class="page-title">
        <h2>Create Taxi Trip</h2>
        <p>Schedule a new route for your assigned vehicle.</p>
    </div>

    @if (TempData["Error"] != null)
    {
        <div class="alert alert-danger">@TempData["Error"]</div>
    }

    <form asp-action="CreateTaxi" method="post" class="form-card">
        <div class="form-grid">
            <div class="field">
                <label asp-for="PickupLocation">Pickup Location</label>
                <input asp-for="PickupLocation" placeholder="Enter pickup location" />
                <span asp-validation-for="PickupLocation" class="text-danger"></span>
            </div>
            <div class="field">
                <label asp-for="DropoffLocation">Dropoff Location</label>
                <input asp-for="DropoffLocation" placeholder="Enter dropoff location" />
                <span asp-validation-for="DropoffLocation" class="text-danger"></span>
            </div>
            <div class="field full">
                <label asp-for="PickupTime">Pickup Date/Time</label>
                <input asp-for="PickupTime" type="datetime-local" step="60" />
                <span asp-validation-for="PickupTime" class="text-danger"></span>
            </div>
            <div class="field">
                <label asp-for="Price">Price (₦)</label>
                <div class="price-input">
                    <span>₦</span>
                    <input asp-for="Price" type="number" step="0.01" placeholder="0.00" />
                </div>
                <span asp-validation-for="Price" class="text-danger"></span>
            </div>
            <div class="field">
                <label asp-for="NumberOfPassengers">Max Passengers</label>
                <input asp-for="NumberOfPassengers" type="number" placeholder="1-5" />
                <span asp-validation-for="NumberOfPassengers" class="text-danger"></span>
            </div>
        </div>
        <button type="submit" class="btn-submit">Create Taxi Trip</button>
    </form>
</div>